## 08:38 · [dono]

- **Agentes de IA — Fase 2B (Buffer de mensagens) planejada, implementada, validada de ponta a
  ponta em produção e desligada de novo por decisão consciente**, tudo na mesma sessão.
  - Planejamento formal (`EnterPlanMode`, dado o tamanho — mesma prática das Fases 1/2A):
    2 perguntas feitas ao Rafael e respondidas — buffer é **opt-in por agente** (toggle novo,
    desligado por padrão, o "Recepção Virtual" já validado continua respondendo na hora até
    alguém ligar) e o poll que processa a janela vencida **só roda em produção**
    (`NODE_ENV === "production"`), nunca em `npm run dev`.
  - Implementado: migração `2026-09-15_v11_agentes_buffer.sql` (2 colunas em `agentes_ia`, 3 em
    `conversas`); `src/lib/agentes-buffer.ts` (novo — abre/estende a janela, poll que fecha
    janelas vencidas e responde, junta as mensagens da rajada numa string só); `src/instrumentation.ts`
    (novo — liga o poll quando o processo sobe); toggle novo no formulário do agente.
    188 testes, typecheck/lint/build limpos. Commitado (`07ded19`).
  - Rafael rodou a migração v11 no SQL Editor do Supabase; deploy no Railway (`6525a599`, sucesso).
  - **Bug real achado no 1º teste ao vivo**: mandei mensagem de teste pelo WhatsApp, o buffer abriu
    e fechou certo (colunas confirmam), mas nenhuma resposta saiu — e nenhum erro apareceu no log.
    Causa: `agente_buffer_desde` era gravado com o relógio no instante em que a função rodava, que é
    sempre um pouco DEPOIS do `created_at` da própria mensagem que abriu a janela (ela já tinha sido
    inserida por outra query, momentos antes) — a busca do poll (`created_at >= agente_buffer_desde`)
    nunca encontrava a mensagem, `juntarMensagensBuffer` recebia lista vazia, e o código tratava
    "nada pra responder" como caso normal (silencioso, sem log de erro).
  - Corrigido: `agente_buffer_desde` passou a gravar com uma folga de 10s pra trás
    (`MARGEM_ABERTURA_MS`), garantindo que a mensagem que abriu a rajada sempre entra na busca.
    188 testes/typecheck/lint/build limpos. Commitado (`ec3d768`) e deployado (`b2dc46d7`, sucesso).
  - **Validado de ponta a ponta com WhatsApp real**: 3 mensagens seguidas em ~3 segundos ("Oie",
    "Oie", "Ola") → 1 resposta só da IA (2 bolhas curtas, que é o "dividir em mensagens curtas"
    normal, não 3 respostas separadas).
  - **Decisão**: Rafael pediu ajuda pra decidir se deixava ligado. Recomendei desligar — nenhum
    paciente real usa o número ainda, o buffer só ajuda quando chegam várias mensagens seguidas (pra
    mensagem única, o caso mais comum, só acrescenta ~10-15s de espera sem ganho), e o próximo marco
    é a demo pro marido, onde resposta rápida pesa mais. Ele topou — buffer desligado de novo no
    Recepção Virtual (`buffer_mensagens: false`, mas `buffer_segundos: 10` fica salvo pra quando
    quiser religar). Decisão completa em `_memoria/decisoes.md`.
  - Achado técnico: sessão de admin mintada localmente (mesmo script/algoritmo de sempre) foi
    **rejeitada pela produção** (401) — o `SESSAO_SECRET` de `crm/.env.local` provavelmente diverge
    do que está no Railway (mesmo tipo de problema já visto antes com o `CRON_SECRET` da Fase 5).
    Contornado lendo/escrevendo direto no Supabase via REST com `SUPABASE_SERVICE_ROLE_KEY` — mesmo
    caminho já documentado em `ferramentas.md`, sem precisar da sessão do painel.
  - Achado técnico: a pedido do Rafael, guiei ele passo a passo (interativo, no VS Code) pra
    autorizar o MCP oficial do Supabase (`supabase-crm-odontominas`, já configurado em `.mcp.json`
    da raiz) via OAuth — funcionou (`/mcp` numa sessão interativa nova). Mas a autorização não
    apareceu nesta sessão em andamento (conexão de MCP carrega só no início da sessão) — deve
    aparecer numa sessão nova a partir daqui. `ferramentas.md` atualizado.

## 13:40 · [dono]

- **Agentes de IA — Prompt estruturado (Simples/Avançado) + aba Conhecimento**, a pedido do Rafael
  (print do "Agente 01" da RoiZap como referência). Perguntado quais das 4 abas novas do print
  (Conhecimento, Qualificação, Ferramentas, Pixel) valiam construir de verdade agora — mesmo
  critério já usado no Chat ao Vivo, sem copiar aba sem funcionalidade real por trás — Rafael
  escolheu só **Conhecimento**.
  - `AgenteForm.tsx` virou 3 abas (Configuração / Prompt do Agente / Conhecimento). Modo Avançado
    preserva o textarea único de sempre — o "Recepção Virtual" em produção continua exatamente
    como estava. Modo Simples estrutura Persona, Objetivo, Fluxo e Triagem, Guardrails (prioridade
    máxima no prompt final, igual ao aviso do print) e Traços de Personalidade (Tom de Voz, Usar
    Emojis). Conhecimento (tabela nova `agentes_conhecimento`) guarda fatos curtos por agente que
    entram no prompt automaticamente, nos dois modos. Cabeçalho do agente ganhou os 4 cards do
    print (Mensagens, Conversas, Tempo Médio, Conhecimentos — dado real) e o Ativar/Pausar subiu
    pro topo.
  - Migração `2026-09-16_v12_agentes_prompt_conhecimento.sql`. **1ª vez que uma migração de schema
    foi aplicada direto pelo MCP do Supabase** (`apply_migration`), sem precisar do SQL Editor
    manual do Rafael — confirma que o MCP recém-autorizado cobre schema também, não só dado.
  - 198 testes/typecheck/lint/build limpos. Deploy no Railway confirmado (`✓ Ready in 772ms`,
    status `SUCCESS` via MCP). Validado ao vivo em produção: página do "Recepção Virtual" renderiza
    certo com a nova UI, agente segue em modo Avançado intacto; criado agente de teste em modo
    Simples com 1 item de Conhecimento pela API, dado conferido certo direto no Supabase, e apagado
    em seguida — banco voltou ao estado de antes.
  - Ainda não commitado nem enviado ao GitHub nesta sessão.

## 14:18 · [dono]

- **Qualificação Automática de Leads construída e em produção** — revertendo, na mesma sessão em
  que perguntei, a decisão de ontem de deixar essa aba fora dos Agentes de IA por falta de
  critério (print da RoiZap). Rafael pediu pra implementar; antes de codar, 2 perguntas resolvidas
  com ele: escala fixa Quente/Morno/Frio (não etiquetas livres por clínica) e reavaliar depois de
  cada resposta do agente (não só na 1ª mensagem) — as 2 recomendadas.
  - `src/lib/agentes-qualificacao.ts` (novo): `classificarQualificacao` reaproveita o mesmo
    provider/modelo já configurado no agente (via `gerarResposta` de `ia-provedores.ts`, prompt
    pedindo 1 palavra só, temperatura 0) — sem infraestrutura nova, só mais uma chamada de IA na
    mesma rodada que já gera a resposta. `parseClassificacao` pura e testada.
    `aplicarQualificacaoAutomatica` garante as 3 etiquetas na clínica (cria a que faltar, cor fixa
    por classificação; reaproveita se já existir uma com o mesmo nome — `etiquetas.ts` ganhou
    `buscarOuCriarEtiqueta`) e aplica só a que bate, removendo as outras duas da conversa — a
    etiqueta mostra sempre a temperatura ATUAL, nunca o histórico de por onde o lead já passou.
  - `src/lib/agentes.ts`: `responderComoAgente` chama a classificação depois de enviar a resposta,
    isolada em try/catch (nunca derruba o envio, que já aconteceu antes) — opt-in por agente
    (`qualificacaoAutomatica`, nasce `false`, não muda nada no Recepção Virtual até alguém ligar).
  - UI (`AgenteForm.tsx`): aba nova "Qualificação" — o toggle do print + as 3 etiquetas explicadas
    quando ligado.
  - Migração `2026-09-16_v13_agentes_qualificacao.sql` (1 coluna em `agentes_ia`, tabela já com
    grant desde a v9). Aplicada direto pelo MCP do Supabase (`apply_migration`), confirmada lendo o
    schema depois — 2ª vez seguida sem precisar do SQL Editor manual do Rafael.
  - Validado: typecheck/lint/202 testes (4 novos: `parseClassificacao`)/build limpos. Deploy no
    Railway (`railway up`) confirmado `SUCCESS` via MCP e `/login` respondendo 200 em produção. Sem
    clique real numa tela (sem `chromium-cli` nesta máquina, limitação já conhecida) — a aba segue
    o mesmo padrão já validado da aba Conhecimento.
  - Ainda não commitado nem enviado ao GitHub nesta sessão.

## 15:04 · [dono]

- **Pixel de Conversão (Facebook Ads + Google Ads) construído e em produção** — revertendo a parte
  do Pixel na decisão de hoje cedo ("só faz sentido quando o tráfego pago começar"). Rafael pediu
  explicitamente pra pesquisar e implementar os dois; antes de codar, 2 perguntas resolvidas com
  ele: os 3 eventos do funil de uma vez (novo lead, lead quente, agendado — não só um) e nasce
  desligado, sem credencial real ainda (mesmo padrão da Qualificação).
  - Pesquisa (Facebook Conversions API + Google Ads, docs atuais) mudou o desenho: a Evolution API
    (Baileys, WhatsApp não-oficial) não recebe `ctwa_clid`/UTMs/`gclid`/`fbclid` — isso só existe
    na API oficial da Meta. As colunas de atribuição (`pacientes.origem_lead`/`utm_*`/`gclid`/
    `fbclid`) nasceram prontas, mas ficam `null` de verdade hoje — dito isso claramente ao Rafael,
    não construído fingindo que funciona. E a rota clássica do Google Ads API pra conversão
    (`OfflineUserDataJobService`) está bloqueada pra conta nova desde abr/jun 2026 — implementação
    foi direto pra **Data Manager API** (`datamanager.googleapis.com/v1/events:ingest`), o caminho
    vigente, sem developer token.
  - `src/lib/pixel-facebook.ts` (novo): Conversions API, hash SHA-256 via Web Crypto (não
    `node:crypto` — mesmo motivo de `sessao.ts`, este arquivo entra na cadeia de import que o
    Next bundla pro cliente via `chat.ts`/`ChatAoVivo.tsx`), `action_source: "business_messaging"`
    (é WhatsApp, não site).
  - `src/lib/pixel-google-ads.ts` (novo): troca refresh token por access token via OAuth2, envia
    pra Data Manager API. Client ID/Secret do app OAuth ficam em env var
    (`GOOGLE_ADS_OAUTH_CLIENT_ID/SECRET`, infra da Noryos, 1 app só); Customer ID, Refresh Token e
    os 3 IDs de Ação de Conversão são por-agente.
  - `src/lib/agentes-pixel.ts` (novo): orquestra os dois, com dedup atômico (`UPDATE ... WHERE
    coluna IS NULL`) — nunca dispara o mesmo evento 2x pra mesma conversa, mesmo sob corrida.
    `ultimo_agente_id` (coluna nova em `conversas`, nunca zera) resolve qual agente é dono da
    conversa pro evento "agendado", que é disparado numa troca manual de status — depois que o
    agente já pode ter parado de escutar.
  - Migração `2026-09-16_v14_agentes_pixel.sql` (9 colunas em `agentes_ia`, 4 em `conversas`, 7 em
    `pacientes`). Aplicada direto pelo MCP do Supabase, confirmada lendo o schema — 3ª vez seguida
    sem SQL Editor manual.
  - UI (`AgenteForm.tsx`): aba "Pixel" — toggle mestre, os 3 cards de evento, campos do Meta
    (Pixel ID + Access Token) e do Google (Customer ID, Login Customer ID opcional, Refresh Token,
    3 IDs de Ação de Conversão).
  - Validado: typecheck/lint/229 testes (27 novos)/build de produção limpos. Deploy no Railway
    (`railway up`) confirmado `SUCCESS` via MCP; webhook em produção respondendo 200 depois do
    deploy.
  - Ainda não commitado nem enviado ao GitHub nesta sessão.

## 18:46 · [dono]

- **Integração ControleODONTO — Fase 0 completa: pesquisa técnica, arquitetura do adapter,
  painel e infraestrutura de sync, em produção.** A pedido do Rafael (brief detalhado pedindo
  camada de integração profissional, sem inventar contrato de API).
  - Pesquisa própria (GitBook oficial, site, terceiros) confirmou o que o brief já suspeitava: a
    trilha `Administração → Serviços Adicionais → Integrações-Webhooks → Autenticações Webhooks`
    do manual oficial existe mas está vazia (título prefixado "(FAZER)", verificado direto nas
    duas páginas). O endpoint de agenda citado no brief
    (`GET /v6/Agendamento/Estabelecimento/{dataInicio}/{dataFim}`) **não foi confirmado de forma
    independente** por esta pesquisa — entrou no código só como candidato, nunca chamado de
    verdade. Nenhuma URL base, autenticação, payload ou endpoint de paciente/escrita confirmado.
    Tudo documentado em `clientes/odontominas/crm/docs/integrations/controle-odonto.md`.
  - Adapter isolado em `src/lib/controle-odonto/` (14 arquivos): `config`, `types`, `errors`,
    `capabilities` (as 7 pedidas, **todas `false`** — só viram `true` manualmente após validação
    real, nunca por env var), `auth` (abstração, sem mecanismo real ainda), `client` (GET com
    timeout 10s, retry em 429/500/502/503/504 com backoff exponencial + jitter, nunca repete
    400/401/403/404), `mapper` (status desconhecido → `unknown_external_status`, mapa vazio de
    propósito), `appointments`/`patients`/`professionals`/`establishments` (interfaces
    preparadas, guardadas por capability), `external-ids` (dedupe genérico), `lock` (lock
    distribuído via chave primária composta), `sync-state` (saúde 🟢🟡🔴⚪), `sync-log`, `sync`
    (janela incremental: margem 24h passado + horizonte 30 dias futuro, configurável),
    `reconcile` (diff local×remoto, nunca apaga sozinho).
  - Migração `2026-09-16_v15_controle_odonto.sql` (`external_ids`, `integration_sync_log`,
    `integration_sync_state`, `integration_locks`, todas com `clinica_id`+RLS+grant) **aplicada
    direto em produção pelo MCP do Supabase**, confirmada lendo o schema (4ª vez seguida sem SQL
    Editor manual).
  - Rotas novas: `POST /api/integrations/controle-odonto/{test,sync}`,
    `GET /api/integrations/controle-odonto/logs`, `POST /api/integrations/controle-odonto/webhook`
    (inerte, responde 200 "desabilitado"), `POST /api/cron/controle-odonto-sync` (reaproveita o
    `CRON_SECRET` já existente, sem pedir segredo novo). Painel: Ferramentas → ControleODONTO
    (status, métricas, Testar conexão/Sincronizar agora/Ver logs) + tela de Logs com "Ver
    detalhes técnicos" recolhível — sem "Reprocessar falhas" de propósito (seria idêntico a
    "Sincronizar agora" hoje, mesmo critério de nunca copiar botão sem funcionalidade real).
  - 59 testes novos (288 no total)/typecheck/lint/build de produção limpos.
  - Deploy no Railway confirmado `SUCCESS` via MCP; smoke test em produção depois do deploy:
    `/login` 200, painel sem sessão redireciona 307, webhook novo responde 200 (desabilitado).
  - Commitado (`265cf80`, só os arquivos desta integração) e enviado ao GitHub via `/syncar` — o
    Pixel de Conversão, que estava pendente de sessão anterior, já tinha sido sincronizado por
    outra sessão em paralelo (commit `e4b11ad`) enquanto esta rodava, sem se misturar.
  - Nenhuma capability real hoje: todo o valor desta entrega é a arquitetura pronta pra ligar
    assim que existir credencial/documentação real do ControleODONTO — próximo passo natural do
    projeto, não uma fase técnica do CRM em si.
- Rodei `/faxina` (escopo memória + contexto): nada crítico — sem segredo fora do lugar, sem
  decisão se contradizendo, sem ferramenta ou automação órfã. Único achado: `_contexto/agora.md`
  tinha passado do teto (71 linhas, teto declarado 40) — cortado nesta mesma sessão, resumindo pro
  essencial (detalhe todo já vive em `clientes/odontominas/andamento.md` e nos diários das datas
  citadas).

## 19:40 · [dono]

- **Análise do briefing de "Disparos" (Ferramentas → Disparos) e Fase A construída.** Rafael trouxe
  um briefing extenso pedindo pra evoluir o módulo pra campanhas/reativação/follow-up de verdade;
  pedi pra analisar antes de programar. Auditoria do repositório encontrou que **o módulo não
  existia**: sem rota, tabela, nav item nem service de Disparos/Campanhas/Fluxos/Listas/Mensagens
  salvas — só a automação fixa de reativação (`reativacao.ts`). Opt-out não existia em lugar
  nenhum; "Funil" no CRM é máquina de estado fixa, não pipeline multi-etapa; só 1 instância de
  WhatsApp; Evolution/Baileys é não-oficial (risco real de bloqueio em disparo grande); `consultas`
  segue sem nenhuma escrita real. Análise entregue por escrito no chat.
  - Rafael sugeriu, de forma independente, um "Motor de Públicos" reutilizável por vários módulos
    (não só Disparos). Recomendei que ele nascesse ANTES do wizard de Disparos, não depois — decisão
    em `_memoria/decisoes.md`.
  - Planejamento formal (`EnterPlanMode`/`ExitPlanMode`): plano fatiado em Fase A (fundamentos) e
    Fase B (Disparos v1 — wizard + worker), salvo em
    `C:\Users\rafaelviriato\.claude\plans\buzzing-imagining-toast.md`. Decisão técnica registrada no
    plano: worker de envio será um scheduler in-process (Railway roda `next start` como processo
    persistente), não GitHub Actions — a granularidade de minutos do cron não serve pro intervalo
    de 15-25s entre mensagens pedido.
  - **Fase A construída e em produção**: migração `v16` (`pacientes.opt_out_em`/`opt_out_origem`,
    tabelas `mensagens_salvas`, `audiencias`), `src/lib/opt-out.ts` (detecção por palavra-chave
    plugada no webhook antes do Agente de IA, com cuidado pra não confundir "posso parar de usar o
    fio dental?" com pedido de opt-out — só substring inequívoco ou palavra sozinha como mensagem
    inteira), `src/lib/mensagens-salvas.ts` (`resolverVariaveis` com fallback seguro, nunca "Olá
    undefined"), `src/lib/audiencias.ts` (motor de públicos v1: etiqueta, status, inatividade;
    opt-out/telefone inválido sempre excluídos). 36 testes novos (324 no total), migração aplicada
    em produção pelo MCP do Supabase (confirmada lendo o schema depois), typecheck/lint/build
    limpos. Ainda não commitado nem sincronizado no GitHub nesta sessão.
  - Próximo passo: Fase B (wizard + worker + relatório), quando o Rafael quiser continuar.

## 20:19 · [dono]

- **Disparos — Fase B (wizard + worker + relatório) construída e em produção**, fechando o módulo
  aberto pela Fase A. Planejamento formal (`EnterPlanMode`/`ExitPlanMode`, mesma prática das fases
  anteriores; plano salvo em `C:\Users\rafaelviriato\.claude\plans\stateless-pondering-prism.md`).
  2 perguntas fechadas com o Rafael antes de codar — as 2 recomendadas: **sem agendamento no v1**
  (campanha só tem "salvar rascunho" ou "criar e iniciar agora") e **sem janela de horário
  comercial** no worker (ele manda a qualquer hora enquanto a campanha estiver "enviando"; quem
  controla a hora de iniciar é quem opera). Decisão completa em `_memoria/decisoes.md`.
  - Migração `2026-09-16_v17_disparos_campanhas.sql` (`campanhas`, `campanha_destinatarios`,
    reaproveitando `integration_locks` já existente com `provider = 'disparos'`).
  - `src/lib/campanhas.ts` (novo): CRUD de campanha + regra pura testável (`montarLinhasDestinatarios`,
    `intervaloEnvioMs`). `src/lib/disparos-worker.ts` (novo): worker de envio in-process, mesmo
    desenho do poll de buffer dos Agentes de IA (`setTimeout` recursivo, ligado por
    `instrumentation.ts`, só em produção) — GitHub Actions não serve pro intervalo de 15-25s entre
    mensagens que o WhatsApp via Evolution exige pra não levar shadowban. O worker reconfere
    opt-out/telefone **ao vivo** antes de cada envio (não confia só no snapshot da criação), grava a
    mensagem no Chat ao Vivo quando há conversa, e só paga o intervalo cheio quando manda mensagem
    de verdade — pular por opt-out ou fechar campanha esgotada agenda o próximo tick rápido.
    `src/lib/disparos-lock.ts` (novo, mesmo padrão do lock do ControleODONTO).
  - Rotas `src/app/api/disparos/*` (campanhas CRUD, iniciar/pausar/retomar/cancelar,
    preview-audiencia, preview-mensagem, criar mensagem salva) e telas `/disparos` (lista),
    `/disparos/nova` (wizard de 3 passos: público → mensagem → revisão, com preview ao vivo) e
    `/disparos/[id]` (relatório com ações e atualização automática enquanto "enviando", reaproveitando
    o `AutoRefresh` que já existia). Item novo no `SidebarNav` (Ferramentas → Disparos).
  - Validado: typecheck/lint/332 testes (4 novos)/build de produção limpos. Migração aplicada em
    produção pelo MCP do Supabase, confirmada lendo o schema — sem novo achado nos advisors de
    segurança. Deploy no Railway (`railway up`) confirmado `SUCCESS` via MCP; logs mostram os dois
    workers (`agentes-buffer` e `disparos-worker`) subindo sem erro; smoke test em produção
    (`/disparos` redireciona 307 sem sessão, `/login` 200).
  - **Falta o teste fim a ponta com paciente real** (criar campanha pequena, ver a mensagem chegar
    no WhatsApp) — decisão do Rafael, não feito sozinho por envolver mandar mensagem real pra
    paciente.
  - Achado fora do escopo desta sessão, não mexido: `.ratosos` sumiu do disco (git mostra como
    deletado, rastreado desde o commit de setup inicial) e apareceu `.noryosinovacoes` (conteúdo
    `2.0`) sem versionar — parece rename/atualização do kit que ficou pela metade.
  - Ainda não commitado nem sincronizado no GitHub nesta sessão.
