## 00:27 · [dono]
- Infra do CRM da OdontoMinas ficou pronta (Fase 1 do plano técnico):
  - Evolution API confirmada no ar na Railway (instância `odontominas-teste`). Chave de API
    testada, QR code gerado, escaneado no número de teste do Rafael — conexão confirmada
    (`connectionState` retornou `open`).
  - Projeto Supabase novo criado (`odontominas-crm`, org noryos-inovacoes, região Americas/São
    Paulo), com "Enable automatic RLS" ligado por recomendação (RLS liga sozinho em toda tabela
    nova) e "Automatically expose new tables" desligado (acesso controlado na mão). Chaves
    (publishable/secret) validadas por chamada direta à API.
  - Credenciais salvas em `clientes/odontominas/crm/.env.local` (fora do git); nunca repetidas no
    chat depois de recebidas.
  - `andamento.md` do projeto e `ferramentas.md` da raiz atualizados ao vivo (Railway + Evolution
    API entraram como ferramentas ligadas).
- Próximo passo: Fase 2 — scaffold do Next.js, primeira migração (`clinicas`, `pacientes`,
  `conversas`, `mensagens`, `eventos_funil`, `consultas`, todas com `clinica_id`) e o endpoint que
  recebe o webhook da Evolution API. Rafael deu sinal verde pra começar.

## 00:36 · [dono]
- MCP do Supabase (projeto `odontominas-crm`, `exaluyarsphucotprwpi`) configurado via
  `claude mcp add --scope project` em `.mcp.json` — acesso completo (docs, account, database,
  debugging, development, functions, branching), não somente-leitura como o outro Supabase
  (Diagnóstico Digital). Falta autorizar por OAuth: precisa rodar `/mcp` numa sessão interativa
  antes de usar.

## 01:43 · [dono]
- **Fase 2 do CRM da OdontoMinas completa e validada de ponta a ponta.** Scaffold Next.js 15 +
  TypeScript + Tailwind em `clientes/odontominas/crm/`; migração V1 (`clinicas`, `pacientes`,
  `conversas`, `mensagens`, `eventos_funil`, `consultas`, todas com `clinica_id`, RLS ligado) em
  `crm/supabase/migrations/`; webhook `/api/webhook/evolution` recebe `messages.upsert`, acha-ou-cria
  paciente/conversa por telefone, avança status `novo→respondido` na 1ª resposta da clínica,
  idempotência por `evolution_message_id`.
- Migração precisou de uma segunda passada: o projeto Supabase não tinha os default privileges do
  schema `public` configurados — toda tabela nova nascia sem `GRANT` nenhum pra `service_role`
  (BYPASSRLS e GRANT são camadas diferentes no Postgres). Corrigido com
  `2026-09-15_v1_grants.sql`.
- Deploy do CRM no Railway (mesmo projeto da Evolution API, `illustrious-perfection`, serviço novo
  `odontominas-crm`), domínio gerado (`odontominas-crm-production.up.railway.app`), webhook
  registrado na instância `odontominas-teste`.
- **Bug real encontrado e corrigido**: o endpoint validava a `apikey` do payload contra a chave
  global da Evolution API, mas a Evolution ecoa o **token da instância** (UUID de 36 caracteres),
  não a chave global (88 caracteres) — todo webhook real tomava 401 em silêncio (sem log). Corrigido
  aceitando as duas (nova env `EVOLUTION_INSTANCE_TOKEN`), com log no caso de rejeição. Diagnosticado
  com uma mensagem direta ao endpoint (bypassando a Evolution) pra isolar se o problema era de rede
  ou de auth.
- Validado com mensagem real de um segundo número (não self-chat — self-chat mostrou disparo de
  webhook inconsistente via Baileys): paciente, conversa e mensagem gravados certos, status mudou
  pra `respondido` quando simulei a resposta da clínica. Dado de teste limpo do Supabase antes de
  fechar a sessão.
- MCP do Supabase (`supabase-crm-odontominas`, HTTP/OAuth) travou em "Pending approval" mesmo
  depois de 3 aprovações do Rafael numa sessão interativa separada — não resolvido, causa não
  identificada. Contornado com um MCP local (`supabase-crm`, `npx @supabase/mcp-server-supabase`)
  autenticado por token de acesso pessoal gerado no dashboard, guardado só em `~/.claude.json`
  (fora do git, escopo `local`) — acesso completo, não somente-leitura.
- A pedido do Rafael ("quero integrar você com a Railway"): CLI da Railway instalada e logada via
  `railway login --browserless` (código de pareamento, sem trava de OAuth), MCP oficial configurado
  via `railway mcp install` (reaproveita a mesma sessão da CLI, também local). Ambos os MCPs novos
  só ficam disponíveis numa sessão futura (esta sessão não recarrega ferramenta nova a meio de
  caminho) — a partir da próxima, dá pra pedir coisa do Supabase/Railway direto, sem SQL Editor.
- Próximo passo: Fase 3 — painel de atendimento (lista de conversas, status, tempo até 1ª
  resposta), a primeira tela de verdade do CRM.

## 02:15 · [dono]
- **Fase 3 do CRM da OdontoMinas completa e validada de ponta a ponta.** Painel de atendimento em
  `clientes/odontominas/crm/`: lista de conversas com paciente/telefone, status (dropdown colorido,
  clicável — a "ação leve" prevista no plano técnico), filtro por status, tempo até 1ª resposta
  calculado a partir de `eventos_funil` (destaque vermelho acima de 30min sem resposta — o "uau" da
  demo), auto-refresh de 20s. Zero dependência nova, só Tailwind. Testado de ponta a ponta com uma
  conversa sintética real no Supabase (criada e apagada na sessão, sem deixar rastro).
- Ao entregar a Fase 3, identifiquei que o painel não tinha login nenhum — qualquer um com a URL do
  Railway via dado de paciente (LGPD). Perguntei ao Rafael sobre estrutura de cargos (ele sugeriu
  Admin, Gestor, Gerente, Dentista, Assistente, Atendente) e sobre prioridade (RBAC completo agora
  vs. remendo mínimo antes da demo). Decisão em `_memoria/decisoes.md`: 2 perfis (`admin`,
  `atendente`), remendo mínimo de senha compartilhada — sem RBAC de verdade ainda, porque hoje as
  duas roles fazem a mesma coisa no painel.
- Implementado: `src/middleware.ts` protege todo o painel e as rotas de API (webhook da Evolution
  API segue público de propósito, é chamada servidor-a-servidor); cookie assinado com HMAC via Web
  Crypto (sem dependência nova, roda em edge runtime); tela de login (`/login`) e logout;
  `.env.example` criado documentando as variáveis novas (`PAINEL_SENHA_ADMIN`,
  `PAINEL_SENHA_ATENDENTE`, `SESSAO_SECRET`).
- Deploy feito no Railway (serviço `odontominas-crm`, `railway up`, aprovado pelo Rafael depois de
  um bloqueio do classificador de modo automático em escrita de variável de ambiente). Validado em
  produção: login errado rejeita, login certo entra com o papel certo, painel exige sessão, webhook
  da Evolution segue aberto, serviço irmão (`evolution-api`) intocado.
- **Atenção:** as senhas de produção hoje são as mesmas temporárias de desenvolvimento
  (`dev-admin-temp`/`dev-atendente-temp`) — o Rafael pediu pra manter assim "por enquanto". Ver
  pendência nova em `agora.md` e no `andamento.md` do projeto.
- Próximo passo: Fase 4 — ficha de paciente + resumo executivo.

## 02:38 · [dono]
- Revisão técnica aprofundada das Fases 1-3 do CRM, a pedido do Rafael ("análise aprofundada,
  melhorias e testes em tudo que foi feito até aqui"). Li o código inteiro (schema, webhook,
  sessão/login, middleware, painel) e achei 2 bugs reais no funil de atendimento:
  - Métrica "tempo até 1ª resposta" contava qualquer saída de `novo` como resposta — marcar uma
    conversa `perdido` direto (sem nunca responder) aparecia como "respondeu em Xmin" no painel.
  - Conversa já resolvida (`respondido`/`agendado`/`perdido`) não reabria quando o paciente
    escrevia de novo — sumia do radar do painel em vez de voltar a aparecer como `novo`.
  Corrigidos os dois: lógica extraída pra `src/lib/funil.ts` (testável isolada), coluna nova
  `aguardando_desde` marca o início do ciclo de espera atual (migração
  `2026-09-15_v2_aguardando_desde.sql`, **ainda não rodada** no Supabase).
- Login: rate limit (5 tentativas erradas / 15min por IP) e comparação de senha em tempo
  constante — as senhas continuam as temporárias, isto só reduz o risco de força bruta enquanto
  isso.
- Testes: Vitest instalado (não existia nenhum teste no projeto), 71 testes novos cobrindo
  normalização de telefone/mensagem do Baileys, formatação, validação de status, assinatura HMAC
  da sessão, rate limit, comparação de senha e o funil/listagem de conversas com um fake de
  Supabase em memória. `typecheck`, `lint` e `next build` limpos.
- `.mcp.json`: escopo do MCP do Supabase enxugado — ver decisão em `_memoria/decisoes.md`.
- **Nada disso está em produção ainda**: falta rodar a migração no SQL Editor e fazer
  `railway up`. Nada foi commitado nem enviado ao GitHub nesta sessão.

## 03:06 · [dono]
- Pendência de produção da revisão técnica fechada: Rafael rodou a migração
  `2026-09-15_v2_aguardando_desde.sql` no SQL Editor do Supabase; `railway up` com os fixes de
  funil/login feito e validado (webhook 200, login 200, painel redireciona 307 sem sessão).
- **Fase 4 do CRM da OdontoMinas completa e validada.** Ficha de paciente (`/pacientes/[id]`):
  dados de contato, status atual, jornada do funil (histórico de transições de `eventos_funil`) e
  histórico de mensagens em bolhas de chat. Resumo executivo (`/resumo`): contagens por status,
  tempo médio até 1ª resposta e alerta de leads esfriando (conversas em aberto há mais de 30min) —
  exatamente o que `contexto.md` já previa que `eventos_funil` deveria alimentar. Painel principal
  agora linka o contato pra ficha e navega pro resumo.
- Lógica de agregação (`calcularResumo`) extraída pura e testável, seguindo o padrão de
  `funil.ts` — 4 testes novos, 75 no total. `typecheck`, `lint` e `next build` limpos. Validado ao
  vivo: com aprovação do Rafael, semeei um paciente/conversa/mensagens sintéticos direto no
  Supabase de produção pra ver a ficha renderizada de verdade (bolhas de chat, jornada), confirmei
  certo e apaguei tudo em seguida.
- **Achado técnico**: nem o CLI local do Supabase (logado numa conta que só enxerga o projeto
  `noryos-inovacoes`, não o `odontominas-crm`) nem o MCP local `supabase-crm` (registrado como
  "ligado" numa sessão anterior) apareceram disponíveis nesta sessão — hoje só o SQL Editor manual
  funciona pra escrever neste banco. Corrigido em `ferramentas.md`.
- Commitado (`29d4187`, "Fase 4 - ficha de paciente + resumo executivo") e deployado no Railway.
- Próximo passo: Fase 5 — 1 automação de destaque (lembrete de consulta ou reativação de paciente
  inativo).

## (cont.) · [dono]
- **Fase 5 do CRM da OdontoMinas completa e em produção**: automação de reativação de paciente
  inativo. Perguntei ao Rafael qual das duas automações construir (lembrete de consulta dependia de
  uma tela nova pra cadastrar consulta, já que `consultas` segue sem nenhuma escrita) — ele escolheu
  reativação, que reaproveita dado que já existe (`conversas`/`mensagens`/`eventos_funil`). Decisão
  completa em `_memoria/decisoes.md`.
- Construído: `src/lib/evolution-send.ts` (envia texto pela Evolution API — contraparte do webhook,
  que só recebia), `src/lib/reativacao.ts` (regra pura: conversa resolvida — respondido/agendado/
  perdido — sem mensagem há mais de 30 dias vira candidata; manda 1x só, nunca repete sozinho +
  orquestração que busca, envia e grava), rota `POST /api/cron/reativacao` (protegida por
  `CRON_SECRET`, mesmo padrão do webhook), migração `2026-09-15_v3_reativacao.sql` (coluna
  `ultima_reativacao_em`). 12 testes novos (87 no total); `typecheck`/`lint`/`next build` limpos.
- Decisão técnica: em vez de um serviço novo no Railway só pra cron (custo/infra extra), o disparo
  diário roda por um workflow do GitHub Actions já commitado
  (`.github/workflows/odontominas-crm-reativacao.yml`, 1x/dia às ~9h Brasília) chamando a rota
  protegida — reaproveita o GitHub que já estava conectado.
- De bônus: achei que `clientes/odontominas/crm/.env.example` nunca tinha sido versionado — o
  `.env*` do `.gitignore` também excluía ele por engano, mesmo sem ter segredo nenhum (só nome de
  variável). Corrigido (`!.env.example`).
- Validação: antes de tocar produção, chequei por leitura direta (REST, sem escrita) que a tabela
  `conversas` do Supabase de produção está vazia — zero risco de mandar mensagem de verdade pra
  paciente real nesta sessão. O classificador de modo automático bloqueou (corretamente) uma
  tentativa minha de chamar a rota com o segredo real pra smoke test, porque isso dispararia a
  automação de verdade — respeitei e segui só com checagens que não executam o envio.
- Deploy: `CRON_SECRET` setado no Railway via MCP (`set-variables`, sem redeploy imediato) e
  `railway up` rodado depois com o código novo. Validado em produção: rota rejeita sem segredo
  (401), painel e webhook seguem intactos.
- Rafael rodou a migração no SQL Editor do Supabase e criou o secret `ODONTOMINAS_CRM_CRON_SECRET`
  no GitHub Actions (mesmo valor do `CRON_SECRET` do Railway) — os dois passos que eu não conseguia
  fazer sozinho (sem MCP do Supabase disponível nesta sessão; sem `gh` CLI instalado nesta máquina).
  Commitado (`c72a211`) e enviado ao GitHub via `/syncar`.
- Próximo passo: Fase 6 — demo pro marido; se validar, demo pra Ariadna.

## (cont.) · [dono]
- Preparo pra Fase 6 (demo): expliquei pro Rafael como abrir o CRM, senha e um roteiro de
  apresentação (painel → ficha → resumo → reativação explicada). Ao testar o login de verdade, o
  painel voltou o erro "não consegui conectar ao banco".
- **Bug real encontrado e corrigido**: os logs do Railway mostraram `PGRST303 "JWT issued at
  future"` — um erro transiente e real do Supabase (bateu de novo a mesma consulta manualmente e
  funcionou normal). O problema é que `src/lib/clinica.ts` guardava esse resultado em cache **pra
  sempre**, mesmo sendo erro — então um soluço passageiro do Supabase travava o painel até o
  processo reiniciar. Corrigido: só guarda em cache o sucesso, nunca a falha. Testado
  (typecheck/lint/test/build limpos) e deployado. Reiniciei o serviço no Railway primeiro (via MCP)
  pra limpar o cache travado na hora, sem esperar o deploy.
- A pedido do Rafael ("admin e atendente têm os mesmos menus, não faz sentido"): Resumo executivo
  virou exclusivo de admin — some do menu e a URL `/resumo` redireciona pro painel se o atendente
  tentar direto (`resumo/page.tsx` faz o gate, não só o menu). Painel principal (conversas) segue
  igual pros dois, é onde o atendente trabalha. Decisão completa em `_memoria/decisoes.md`.
- Semeei 5 conversas fictícias direto no Supabase de produção (aprovação do Rafael), pra ele ver o
  painel funcionando de verdade em vez de vazio: Camila Duarte (novo), Rodrigo Alves (aguardando
  52min, aparece em vermelho), Fernanda Lima (respondido em 8min), Marcos Teixeira (agendado, com
  jornada respondido→agendado), Beatriz Nogueira (perdido, inativa há 47 dias — marcada
  propositalmente com `ultima_reativacao_em` já preenchida, pra não disparar mensagem de verdade no
  cron de amanhã de manhã). Validei ao vivo por curl com cookie de sessão: admin vê Resumo e entra;
  atendente não vê o link e toma redirect se tentar a URL; os 5 nomes aparecem certos no painel.
  **Pendência nova**: decidir se apaga esses dados fictícios antes da demo real ou mantém como
  dado fixo de demonstração.
- 2 deploys nesta sessão: 1 só com a correção do cache, 1 com o RBAC do Resumo.

## (cont.) · [dono]
- A pedido do Rafael ("a V1 ficou sem muita informação, a dona da clínica precisa ser impactada
  mesmo numa V1"): pacote de melhorias na V1 do painel do CRM, todas verificadas ao vivo contra o
  Supabase e a Evolution API de produção (não só teste automatizado):
  - **Menu lateral** em toda tela logada (Painel, Equipe, Resumo Executivo, Conexão WhatsApp — as
    3 últimas só admin, com gate de verdade no servidor, não só escondendo o link — confirmado
    tentando acessar `/equipe`/`/resumo`/`/conexao` logado como atendente: redireciona).
  - **Login por atendente** substitui as 2 senhas compartilhadas: tabela nova `atendentes` (nome,
    usuário, senha com hash scrypt, papel) — migração `2026-09-15_v4_equipe.sql`, **ainda não
    rodada em produção** (pendência nova abaixo). Cookie de sessão passa a carregar quem entrou
    (id + nome), não só o papel.
  - **Equipe** (tela nova, admin): atendimentos hoje e no total por secretária, tempo médio até
    responder, última atividade. Toda troca manual de status no painel agora grava quem fez
    (`eventos_funil.atendente_id` novo); resposta automática do webhook continua sem dono, porque
    o sistema não tem como saber qual secretária digitou a resposta no WhatsApp.
  - **Conexão WhatsApp** (tela nova, admin): status ao vivo (bolinha verde/vermelha + número) e QR
    Code pra reconectar direto do painel, sem abrir Railway/Evolution. Confirmei que está
    genuinamente conectado agora, número real (61) 9925-6901.
  - Sidebar mostra sempre quem está logado (bolinha verde + nome do atendente) e o status da
    conexão do WhatsApp — pedido explícito do Rafael, pensando nas várias secretárias da clínica.
  - Ficha de paciente: a jornada agora mostra quem atendeu cada troca de status.
  - Validação ao vivo: 103 testes, `typecheck`/`lint`/`next build` limpos, e sessões reais
    (assinadas com o mesmo `SESSAO_SECRET` de produção) usadas pra navegar como admin e como
    atendente contra o Supabase e a Evolution API de verdade — confirmando menu, gate de admin,
    conexão WhatsApp real e as páginas novas com dado real da clínica (não mock). Sem a migração
    v4, login degrada pra "credenciais inválidas" com log claro (`PGRST205`, tabela ausente) em vez
    de quebrar — confirmado tentando logar antes de rodar a migração.
- **Pendência nova**: rodar `2026-09-15_v4_equipe.sql` no SQL Editor do Supabase e fazer
  `railway up`. A migração semeia 3 contas de demonstração (`admin`/`admin-temp-2026`,
  `recepcao1`/`recepcao1-temp-2026`, `recepcao2`/`recepcao2-temp-2026`) — trocar usuário e senha
  pelas secretárias reais antes da demo. Substitui a pendência antiga "trocar as senhas
  temporárias compartilhadas" (resolvida pela própria mudança de arquitetura, não só trocada).
- Nada commitado nem enviado ao GitHub nesta sessão.
