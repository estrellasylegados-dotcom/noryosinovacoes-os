# 2026-09-20

## OdontoMinas — Automações comerciais por Kanban

- A migration `v36` foi aplicada no Supabase de produção e validada depois da execução. Estrutura, funções, índices, gatilhos e RBAC ficaram disponíveis; as contagens antes e depois permaneceram iguais: 8 execuções de fluxo, 31 eventos, 18 eventos de automação, 16 oportunidades, 18 pacientes, 65 conversas, 8 fluxos e 8 versões. Não apareceram referências inválidas nem novas entradas no outbox durante a migração.
- A validação autenticada foi feita com a conta `[TESTE] Noryos Admin`. Pela interface, foi criado o fluxo `[TESTE AUTOMAÇÃO KANBAN]` (`ba51dbe3-93c1-4ead-aa8c-0ef3b823407b`), baseado no modelo de follow-up de orçamento, com gatilho de entrada no estágio Follow-up do pipeline Comercial, interrupção por mudança de estágio ou resposta do paciente, respeito ao horário e reentrada uma vez por entrada no estágio. As duas esperas foram reduzidas para 15 segundos, a validação sem envio passou e a versão `6e510b69-ac9a-4e56-81c3-2f6ec7ae80e7` foi publicada.
- A tentativa de ativação pela interface, com `AUTOMACOES_KANBAN_ENABLED=false` no Railway, foi corretamente recusada pelo servidor, mas a tela ocultava o erro e parecia apenas continuar pausada. O componente `crm/src/components/fluxos/FluxoAcoes.tsx` foi corrigido localmente para tratar o status/JSON da resposta e exibir uma mensagem amigável.
- O teste PostgreSQL isolado `src/lib/fluxo-comercial-postgres.test.ts` passou com 11/11 casos, cobrindo transação e rollback do outbox, deduplicação, concorrência de claim, cancelamento, resposta, espera por entrada, fixação de versão e isolamento por clínica.
- No E2E controlado em produção, foi usada somente a oportunidade do paciente de teste autorizado e enviado exatamente um WhatsApp para `5561981925241`. Evidências: execução `91d2734a-a95c-41c8-93f0-94a0d1c8482a`, evento `29802eb2-aa0e-4474-9c97-f4e9f9ce84f6`, oportunidade `530a25a7-6f88-4b93-bc70-dfc1bbf66089`, paciente `63cd3fa3-624c-46f5-a58c-3a0ad94292b8`, conversa `0ee7964f-d8b3-421b-a4d2-1a8083058b97`, canal `a20d94ac-8765-43e0-8abb-868c624e0b67`, dedupe `kanban:530a25a7-6f88-4b93-bc70-dfc1bbf66089:94831765-bdee-4a06-834d-4a27c2044a1c` e mensagem do provedor `3EB0FC9B48A7648BA867C3401C371A33A2DF617A`.
- A sequência registrada foi: início, primeira espera, condição avaliada, mensagem confirmada e segunda espera. Ao final, o fluxo foi pausado, a execução ficou interrompida manualmente e nenhuma automação permaneceu ativa.
- O histórico mostrava UUID da oportunidade e motivo técnico cru. `crm/src/components/fluxos/FluxoHistoricoExecucoes.tsx` foi corrigido localmente para apresentar motivos operacionais em português e deixar o UUID nos detalhes técnicos recolhidos.
- A interface ficou adequada no desktop. Em 768 px, a lista depende de rolagem horizontal e o editor fica apertado, portanto ainda precisa de ajuste responsivo antes da liberação operacional.
- Evidências preservadas em `crm/docs/AUTOMACOES-KANBAN-VALIDACAO-E2E-2026-09-20.md`; roteiro preservado em `crm/scripts/e2e-automacoes-kanban-prod.ts`.
- Gates finais aprovados: typecheck, lint, build com 45 páginas e suíte completa com 75 arquivos/931 testes.
- Resultado: motor aprovado, mas funcionalidade reprovada para liberação operacional enquanto a flag do Railway continuar desligada, as correções locais não forem publicadas e a experiência em 768 px não for ajustada e revalidada.
- Não houve `git push`, deploy no Railway nem qualquer envio adicional de WhatsApp.


## 12:14 · dono

- Noryos Ops V1 foi implementado localmente no CRM da OdontoMinas como central interna de operações da plataforma.
- Reutilizado: `clinicas`, `canais`, Central de Alertas, `auditoria_eventos`, `automacao_eventos` e `integration_sync_state`/`integration_sync_log`.
- Criado: migration aditiva `crm/supabase/migrations/2026-09-20_v37_noryos_ops.sql`, serviço `crm/src/lib/noryos-ops.ts`, telas `/ops`, `/ops/clinicas`, `/ops/canais`, `/ops/integracoes`, `/ops/workers`, `/ops/erros`, `/ops/incidentes`, `/ops/deploys`, `/ops/auditoria`, APIs de incidentes em `/api/ops/incidentes`, e tabelas `ops_erros`, `ops_incidentes`, `ops_worker_heartbeats`, `ops_deploys`.
- RBAC: novas permissões `ops.*`; Noryos Admin tem acesso completo, Noryos Suporte tem acesso autorizado, e perfis da clínica (`dona`, `gerente`, `supervisora`, `atendente`) ficam bloqueados por backend e UI.
- Segurança: erros técnicos passam por sanitização antes de aparecer no Ops; segredo/API key não é exposto; incidentes são a única escrita operacional da V1.
- Gates aprovados: `npm run typecheck`, `npm run lint`, `npm test` com 76 arquivos/939 testes e `npm run build` com 55 páginas.
- Validação visual autenticada ficou limitada: a ferramenta de navegador retornou `Browser is not available: iab`; checagem HTTP local das rotas `/ops/*` sem sessão retornou 307 para login, como esperado.
- Não houve commit, push, deploy, aplicação da migration v37 no Supabase nem publicação no Railway.
