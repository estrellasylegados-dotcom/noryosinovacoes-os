# 2026-09-12

## dono

- Site da OdontoMinas: primeiro deploy de produção. Rafael conectou o repositório
  `noryosinovacoes-os` à Cloudflare Pages pelo dashboard (root `clientes/odontominas/site`, build
  `npm run build`, saída `out`, deploy automático a cada push na `main`). Confirmei que carregou
  certo — título, dados da clínica e a seção do Protocolo Correct visíveis, sem erro aparente —
  em https://odontominas.pages.dev/. Registrado em `infra.md`, `ferramentas.md`,
  `clientes/odontominas/andamento.md` e `agora.md` (pendência de deploy sai, resolvida).
