## 11:58 · [dono]
- CRM OdontoMinas, Fluxo de Conversa: Fase 0 (auditoria só-leitura) concluída — módulo não existia
  em nenhuma camada do sistema, mesmo achado de Campanhas. Relatório em
  `crm/docs/fluxo-conversa-auditoria-fase0.md`; visão completa do Rafael preservada em
  `crm/docs/fluxo-conversa-visao.md`.
- 3 perguntas em aberto fechadas antes da Fase 1: `reativacao.ts` migra pro motor novo eventualmente
  (não agora); 5 riscos já existentes no código viram patches separados (2 moldam o desenho do
  motor); nomenclatura das tabelas em português.
- Fase 1 (arquitetura/schema): proposta entregue, revisão de arquitetura (agente Plan) e depois
  revisão final pedida pelo Rafael em 5 pontos (created_at/updated_at, índices, FKs/ON DELETE
  preservando histórico, is_test, recovery) — achou 3 problemas reais, corrigidos. Migration `v20`
  aplicada em produção via MCP do Supabase, confirmada lendo o schema.
- Fase 2a (núcleo do motor, sem editor visual): planejada formalmente, com revisão de arquitetura
  que achou 2 bugs antes de codar (query de claim que nunca reivindicaria menu sem timeout; estado
  `running` sem caminho de recovery). Construída: `dono-conversa.ts` + 6 sítios de escrita
  corrigidos (4 em `agentes.ts`, 1 em `etiquetas.ts`, 1 em `chat.ts` — este achado só na revisão);
  `fluxo-tipos`/`fluxo-validador`/`fluxo-motor`/`fluxo-gatilhos` (puro, 65 testes novos, sem
  depender de `zod`); `fluxo-execucoes`/`fluxo-lock`/`fluxo-worker` (I/O, sem CTE/RPC — este projeto
  nunca usou função de banco); webhook integrado. 381 testes, typecheck/lint/build limpos.
- Deploy no Railway (`railway up`). Validação com fixture achou 1 bug real (embed ambíguo do
  PostgREST entre `fluxo_execucoes`↔`conversas`, erro nunca checado) — corrigido, redeployado,
  revalidado: execução completou os 4 passos certos. Dados de teste apagados ao final.
- Teste real de ponta a ponta, aprovado pelo Rafael: fluxo "TESTE - Fluxo Odonto" (Início→Mensagem→
  Finalizar) rodou sozinho em produção, mandou mensagem de verdade pro WhatsApp de teste —
  `evolution_message_id` confirmado pela Evolution. Fluxo arquivado (não apagado) ao final.
- Fase 0, 1 e 2a do Fluxo de Conversa completas e em produção. Próximo passo: Fase 2b/3 (editor
  visual), sem data definida.

## 13:00 · [dono]
- Fluxo de Conversa: Fase 2b/3 (editor visual) construída — Rafael pediu pra avançar direto pra
  ela em vez da Fase 6 (demo), pra poder mostrar "criar fluxo → arrastar blocos → conectar →
  publicar → receber mensagem real" em vez de só backend. Planejamento formal (`EnterPlanMode`,
  2 agentes Explore + 1 agente Plan) antes de codar.
- `@xyflow/react` aprovado como única exceção à política de zero-dependência do projeto, pro
  canvas (zoom/pan/minimap/conexões) — motor (`fluxo-motor`/`fluxo-tipos`/`fluxo-worker`) intocado.
  2 ajustes cirúrgicos em `fluxo-execucoes.ts` (`isTest`/`versaoIdForcada` em
  `iniciarExecucaoFluxo`, só ativos quando `isTest=true`) e `fluxo-validador.ts` (`validarGrafo`
  ganhou `{noIds, mensagem}` estruturado, texto idêntico) — confirmado que o único call site real
  (webhook) segue chamando exatamente como antes.
- Construído: 3 libs puras novas (ponte nodes/edges do xyflow↔`NoFluxo`, layout automático em
  camadas, 3 templates odontológicos combinando os 6 tipos de nó existentes — sem tipo de nó
  novo), 3 libs de I/O (CRUD/versionamento rascunho→publicada→substituída, busca de contato de
  teste sem número hardcoded), 9 rotas de API, 3 páginas, 13 componentes (canvas, paleta com
  categoria Odonto visível-mas-desabilitada, painel de validação clicável, modo teste com
  timeline ao vivo, undo/redo, autosave). 49 testes novos (430 no total). typecheck/lint/build
  limpos.
- Deploy em produção via Railway (`railway up`), 2 vezes. O teste real de ponta a ponta (navegador
  automatizado — Playwright, credencial de admin fornecida pelo Rafael) achou um bug real: o
  painel "Testar" ficava preso em "na fila" por um closure desatualizado no polling
  (`FluxoPainelTeste.tsx` lia `execucaoId` do estado React de dentro do `setInterval`, que nunca
  via o valor atualizado). O backend funcionou certinho o tempo todo (confirmado direto no banco:
  execução `completed`, 3 eventos sem erro, `evolution_message_id` presente) — só a tela não
  atualizava. Corrigido (passar o id da execução direto pro polling, sem depender do estado
  assíncrono) e redeployado.
- Teste real de ponta a ponta pelo próprio editor (não mais só via fixture/DB direto como na Fase
  2a): fluxo "TESTE - Fluxo Odonto" criado arrastando um bloco Mensagem da paleta, conectado
  Início→Mensagem→Finalizar no canvas, testado contra "Rafael (teste Disparos)" (busca por nome,
  sem número fixo no código) — mensagem chegou de verdade no WhatsApp. Fluxo arquivado ao final,
  dados de teste ficam no banco por enquanto (mesma política já usada em Disparos/Campanhas/Fase 2a).
- Fase 2b/3 completa e validada em produção. Próximo passo: ampliar a paleta (blocos Odonto reais
  quando o ControleODONTO estiver validado) ou Fase 6 (demo pro marido) — nenhuma das duas
  decidida ainda.
