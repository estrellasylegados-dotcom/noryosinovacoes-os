## 11:58 · [dono]
- CRM OdontoMinas, Fluxo de Conversa: Fase 0 (auditoria só-leitura) concluída — módulo não existia
  em nenhuma camada do sistema, mesmo achado de Campanhas. Relatório em
  `crm/docs/fluxo-conversa-auditoria-fase0.md`; visão completa do Rafael preservada em
  `crm/docs/fluxo-conversa-visao.md`.
- 3 perguntas em aberto fechadas antes da Fase 1: `reativacao.ts` migra pro motor novo eventualmente
  (não agora); 5 riscos já existentes no código viram patches separados (2 moldam o desenho do
  motor); nomenclatura das tabelas em português.
- Fase 1 (arquitetura/schema): proposta entregue, revisão de arquitetura (agente Plan) e depois
  revisão final pedida pelo Rafael em 5 pontos (created_at/updated_at, índices, FKs/ON DELETE
  preservando histórico, is_test, recovery) — achou 3 problemas reais, corrigidos. Migration `v20`
  aplicada em produção via MCP do Supabase, confirmada lendo o schema.
- Fase 2a (núcleo do motor, sem editor visual): planejada formalmente, com revisão de arquitetura
  que achou 2 bugs antes de codar (query de claim que nunca reivindicaria menu sem timeout; estado
  `running` sem caminho de recovery). Construída: `dono-conversa.ts` + 6 sítios de escrita
  corrigidos (4 em `agentes.ts`, 1 em `etiquetas.ts`, 1 em `chat.ts` — este achado só na revisão);
  `fluxo-tipos`/`fluxo-validador`/`fluxo-motor`/`fluxo-gatilhos` (puro, 65 testes novos, sem
  depender de `zod`); `fluxo-execucoes`/`fluxo-lock`/`fluxo-worker` (I/O, sem CTE/RPC — este projeto
  nunca usou função de banco); webhook integrado. 381 testes, typecheck/lint/build limpos.
- Deploy no Railway (`railway up`). Validação com fixture achou 1 bug real (embed ambíguo do
  PostgREST entre `fluxo_execucoes`↔`conversas`, erro nunca checado) — corrigido, redeployado,
  revalidado: execução completou os 4 passos certos. Dados de teste apagados ao final.
- Teste real de ponta a ponta, aprovado pelo Rafael: fluxo "TESTE - Fluxo Odonto" (Início→Mensagem→
  Finalizar) rodou sozinho em produção, mandou mensagem de verdade pro WhatsApp de teste —
  `evolution_message_id` confirmado pela Evolution. Fluxo arquivado (não apagado) ao final.
- Fase 0, 1 e 2a do Fluxo de Conversa completas e em produção. Próximo passo: Fase 2b/3 (editor
  visual), sem data definida.

## 13:00 · [dono]
- Fluxo de Conversa: Fase 2b/3 (editor visual) construída — Rafael pediu pra avançar direto pra
  ela em vez da Fase 6 (demo), pra poder mostrar "criar fluxo → arrastar blocos → conectar →
  publicar → receber mensagem real" em vez de só backend. Planejamento formal (`EnterPlanMode`,
  2 agentes Explore + 1 agente Plan) antes de codar.
- `@xyflow/react` aprovado como única exceção à política de zero-dependência do projeto, pro
  canvas (zoom/pan/minimap/conexões) — motor (`fluxo-motor`/`fluxo-tipos`/`fluxo-worker`) intocado.
  2 ajustes cirúrgicos em `fluxo-execucoes.ts` (`isTest`/`versaoIdForcada` em
  `iniciarExecucaoFluxo`, só ativos quando `isTest=true`) e `fluxo-validador.ts` (`validarGrafo`
  ganhou `{noIds, mensagem}` estruturado, texto idêntico) — confirmado que o único call site real
  (webhook) segue chamando exatamente como antes.
- Construído: 3 libs puras novas (ponte nodes/edges do xyflow↔`NoFluxo`, layout automático em
  camadas, 3 templates odontológicos combinando os 6 tipos de nó existentes — sem tipo de nó
  novo), 3 libs de I/O (CRUD/versionamento rascunho→publicada→substituída, busca de contato de
  teste sem número hardcoded), 9 rotas de API, 3 páginas, 13 componentes (canvas, paleta com
  categoria Odonto visível-mas-desabilitada, painel de validação clicável, modo teste com
  timeline ao vivo, undo/redo, autosave). 49 testes novos (430 no total). typecheck/lint/build
  limpos.
- Deploy em produção via Railway (`railway up`), 2 vezes. O teste real de ponta a ponta (navegador
  automatizado — Playwright, credencial de admin fornecida pelo Rafael) achou um bug real: o
  painel "Testar" ficava preso em "na fila" por um closure desatualizado no polling
  (`FluxoPainelTeste.tsx` lia `execucaoId` do estado React de dentro do `setInterval`, que nunca
  via o valor atualizado). O backend funcionou certinho o tempo todo (confirmado direto no banco:
  execução `completed`, 3 eventos sem erro, `evolution_message_id` presente) — só a tela não
  atualizava. Corrigido (passar o id da execução direto pro polling, sem depender do estado
  assíncrono) e redeployado.
- Teste real de ponta a ponta pelo próprio editor (não mais só via fixture/DB direto como na Fase
  2a): fluxo "TESTE - Fluxo Odonto" criado arrastando um bloco Mensagem da paleta, conectado
  Início→Mensagem→Finalizar no canvas, testado contra "Rafael (teste Disparos)" (busca por nome,
  sem número fixo no código) — mensagem chegou de verdade no WhatsApp. Fluxo arquivado ao final,
  dados de teste ficam no banco por enquanto (mesma política já usada em Disparos/Campanhas/Fase 2a).
- Fase 2b/3 completa e validada em produção. Próximo passo: ampliar a paleta (blocos Odonto reais
  quando o ControleODONTO estiver validado) ou Fase 6 (demo pro marido) — nenhuma das duas
  decidida ainda.

## 13:36 · [dono]
- `.ratosos` renomeado pra `.noryosinovacoes` (Rafael já tinha feito manualmente) — alinhadas as
  referências que ainda apontavam pro nome antigo: mapa de pastas do `AGENTS.md`, checagem de
  integridade em `conferir-kit.sh`, estrutura mínima checada pelo `/setup`. `sistema/changelog/`
  intocado de propósito (gabarito genérico do kit, sobrescrito a cada atualização). Commit `2fff7ed`.
- Fluxo de Conversa (CRM OdontoMinas): ampliação da paleta do editor com 2 fatias novas, mesma
  disciplina das fases anteriores (typecheck/lint/build/testes a cada corte, sem tocar produção).
  - **Ações CRM** (5 blocos: adicionar/remover etiqueta, mover no funil, marcar prioridade, atribuir
    atendente): auditoria achou a categoria Odonto 100% bloqueada (nenhuma capability do
    ControleODONTO confirmada, sem meio-termo) e que as outras 3 categorias da visão original
    (Ações CRM/Humano/IA/Integração) não dependem disso — só usam o que o CRM já tem em produção.
    Sem migration. Achado evitado na implementação: reusar `chat.ts:atualizarConversaChat` pra
    prioridade/atendente criaria um ciclo de import (`chat.ts` já importa de `fluxo-execucoes.ts`) —
    escrito direto em `conversas` em vez disso. typecheck/lint/build limpos; 449 testes (19 novos).
    Commit `c3d04e8`.
  - **Humano + IA** (4 blocos: transferir p/ humano, criar alerta interno, pausar automação, iniciar
    agente de IA): auditoria achou que "Encerrar IA"/"Retomar fluxo após IA"/"Enviar contexto pra
    agente" pressupõem um protocolo de handoff `agentes.ts` ↔ motor do fluxo que não existe —
    enquanto um nó do fluxo executa, `dono_conversa` já é `'fluxo'`, não há "IA ativa durante um
    passo" pra encerrar ou retomar. Rafael confirmou deixar só os 4 blocos seguros; os outros 3
    ficam documentados como fase separada (protocolo de retorno IA→motor, contexto, idempotência,
    concorrência). 2 bugs reais achados e corrigidos antes de qualquer deploy: (1) o
    `liberarControle` genérico que já roda ao terminar qualquer execução sempre devolvia a conversa
    pro humano — sem ajuste, ele stompearia a entrega pro agente logo depois de `iniciar_agente_ia`
    (corrigido: `aplicarAcaoCrm` avisa quando ela mesma já transferiu o dono, e a terminação pula o
    `liberarControle` nesse caso); (2) a checagem "algum finalizar alcançável" do validador de grafo
    só olhava `tipo==='finalizar'`, gerando aviso falso em fluxo terminando por
    `transferir_humano`/`iniciar_agente_ia` (corrigido com o helper `ehNoTerminal`). Sem migration.
    typecheck/lint/build limpos; 464 testes (15 novos). Commit `0fad463`.
  - Categoria **Integração** (Webhook/Chamada API/Consultar sistema/Aguardar callback) pausada por
    decisão do Rafael: precisa de schema novo (cofre de credenciais, que não existe hoje pra
    integrações de terceiros) e abre risco real de SSRF (servidor chamando URLs configuradas dentro
    de um fluxo) — classe de risco diferente das duas fatias anteriores. Fica documentada como
    próxima fase específica.
  - **Nada disso foi aplicado em produção real nesta sessão**: 3 commits locais (`2fff7ed`,
    `c3d04e8`, `0fad463`), ainda não sincronizados com o GitHub nem deployados no Railway. O teste
    robusto ponta a ponta (navegador real + WhatsApp real — criação no editor, publicação, execução
    pelo motor, Ações CRM, Humano+IA, waits/condições, persistência após restart, idempotência,
    ausência de duplicidade, opt-out, tratamento de erro, logs) continua reservado pro fim de todas
    as fases da ampliação da paleta.

## 16:50 · [dono]
- `/syncar` rodado: os 3 commits acima + a atualização de memória foram pro GitHub (`cd88695`).
  Validado: `main` = `origin/main`, working tree limpa, nenhum `.env` versionado, sem segredo.
- **Fase 6 (demo pro marido) iniciada.** Deploy real no Railway via `railway up` (commit `cd88695`)
  — **Ações CRM e Humano+IA foram ao ar pela 1ª vez**, deployment `3570f47e` SUCCESS, logs limpos,
  os 3 workers (`agentes-buffer`/`disparos-worker`/`fluxo-worker`) subiram sem erro.
- Fluxo **"DEMO - Atendimento Odontológico"** criado e publicado direto no banco (não pela UI —
  sem a senha do painel nesta sessão, ver `ferramentas.md`): 29 nós, gatilho manual (nunca inicia
  sozinho por mensagem real), cobrindo 8 dos 9 blocos novos (só "remover etiqueta" ficou de fora).
  Validado antes de publicar com os validadores reais do projeto (`validarFormaDefinicao`/
  `validarGrafo`, via teste temporário deletado depois) — zero erros, zero avisos. Corrigi um erro
  de sequência que eu mesmo tinha sugerido antes (transferir→alerta→pausa não funciona porque
  `transferir_humano` é terminal; ficou alerta→pausa→transferir). Criadas 4 etiquetas
  odontológicas novas (Implantes/Ortodontia/Estética/Clínica Geral).
- **Teste real pontual disparado, ainda pendente.** Execução `a98ee509` rodando contra o número de
  teste já conhecido (5561981925241): as 2 primeiras mensagens confirmadas enviadas de verdade
  (evolution_message_id). Parado em `waiting_input` no `menu_principal`, esperando o Rafael
  responder "1" no WhatsApp — não é simulável, só uma resposta real resolve o nó.
- Documento `crm/docs/demo-fase6-roteiro.md` criado: roteiro de 10-15min (9 blocos), os 2
  personagens da demo (fluxo ao vivo + os 5 pacientes fictícios já cobrindo as 5 etapas do funil),
  detalhamento dos 4 caminhos, e checklist pré-apresentação.
- **Nova frente aberta, ainda em auditoria — não implementada.** Rafael pediu pesquisa de mercado
  (CRM/relacionamento odontológico, Brasil + internacional) pra achar funcionalidade que falta pra
  impressionar a cliente-piloto. 10 candidatas priorizadas (avaliação Google, NPS, aniversário,
  indicação, catálogo no WhatsApp, dashboard visual, proposta digital de tratamento, marca/tema por
  clínica, gamificação de atendente, multi-unidade), todas fora de prontuário/agenda/financeiro
  (isso é ControleODONTO). Rafael batizou a frente de **"Noryos Odonto"** e decidiu implementar
  agora 4 delas: NPS, avaliação Google, aniversário e dashboard executivo.
- Auditoria técnica (Fase 1) feita antes de codar qualquer coisa: `reativacao.ts` é o precedente
  real de "gatilho por data" (cron → seleciona candidato → manda → registra); `resumo.ts` já é o
  embrião do dashboard (contagem de funil + `leadsEsfriando`, que já é a seção "atenção
  necessária"); `pacientes.origem_lead`/`utm_*` já existem (cobre a seção "origem" do dashboard
  quase de graça); **`pacientes.data_nascimento` não existe em lugar nenhum do sistema** (bloqueia
  aniversário até decidir de onde esse dado vem); achados 3 pontos de "OdontoMinas" hardcoded
  (2 de UI, 1 dentro do texto da mensagem de reativação — achado real de white-label). Proposta:
  estender o motor do Fluxo de Conversa (gatilho novo + 1 tipo de nó novo de captura de resposta)
  em vez de um motor de automação paralelo — **pergunta em aberto pro Rafael, ainda sem resposta.**
