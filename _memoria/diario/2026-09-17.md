## 11:58 · [dono]
- CRM OdontoMinas, Fluxo de Conversa: Fase 0 (auditoria só-leitura) concluída — módulo não existia
  em nenhuma camada do sistema, mesmo achado de Campanhas. Relatório em
  `crm/docs/fluxo-conversa-auditoria-fase0.md`; visão completa do Rafael preservada em
  `crm/docs/fluxo-conversa-visao.md`.
- 3 perguntas em aberto fechadas antes da Fase 1: `reativacao.ts` migra pro motor novo eventualmente
  (não agora); 5 riscos já existentes no código viram patches separados (2 moldam o desenho do
  motor); nomenclatura das tabelas em português.
- Fase 1 (arquitetura/schema): proposta entregue, revisão de arquitetura (agente Plan) e depois
  revisão final pedida pelo Rafael em 5 pontos (created_at/updated_at, índices, FKs/ON DELETE
  preservando histórico, is_test, recovery) — achou 3 problemas reais, corrigidos. Migration `v20`
  aplicada em produção via MCP do Supabase, confirmada lendo o schema.
- Fase 2a (núcleo do motor, sem editor visual): planejada formalmente, com revisão de arquitetura
  que achou 2 bugs antes de codar (query de claim que nunca reivindicaria menu sem timeout; estado
  `running` sem caminho de recovery). Construída: `dono-conversa.ts` + 6 sítios de escrita
  corrigidos (4 em `agentes.ts`, 1 em `etiquetas.ts`, 1 em `chat.ts` — este achado só na revisão);
  `fluxo-tipos`/`fluxo-validador`/`fluxo-motor`/`fluxo-gatilhos` (puro, 65 testes novos, sem
  depender de `zod`); `fluxo-execucoes`/`fluxo-lock`/`fluxo-worker` (I/O, sem CTE/RPC — este projeto
  nunca usou função de banco); webhook integrado. 381 testes, typecheck/lint/build limpos.
- Deploy no Railway (`railway up`). Validação com fixture achou 1 bug real (embed ambíguo do
  PostgREST entre `fluxo_execucoes`↔`conversas`, erro nunca checado) — corrigido, redeployado,
  revalidado: execução completou os 4 passos certos. Dados de teste apagados ao final.
- Teste real de ponta a ponta, aprovado pelo Rafael: fluxo "TESTE - Fluxo Odonto" (Início→Mensagem→
  Finalizar) rodou sozinho em produção, mandou mensagem de verdade pro WhatsApp de teste —
  `evolution_message_id` confirmado pela Evolution. Fluxo arquivado (não apagado) ao final.
- Fase 0, 1 e 2a do Fluxo de Conversa completas e em produção. Próximo passo: Fase 2b/3 (editor
  visual), sem data definida.
